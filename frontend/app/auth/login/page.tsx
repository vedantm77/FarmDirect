'use client';
import {useState} from 'react';
const api=process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000';
export default function Login(){const[email,setEmail]=useState('buyer@farmdirect.demo'),[password,setPassword]=useState('FarmDirect2026!'),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const r=await fetch(`${api}/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw Error(d.detail);sessionStorage.setItem('farmdirect-token',d.access_token);setMessage(`Signed in as ${d.user.name} (${d.user.role}).`)}catch{setMessage('Backend unavailable — use the SIH demo workflow on the home page.')};}return <main className="app"><nav className="nav"><div className="brand"><i/>FarmDirect</div><span className="pill">DEMO CREDENTIALS READY</span></nav><section className="card" style={{maxWidth:480,margin:'40px auto'}}><h1>Welcome back</h1><p>Sign in to your Farmer, Buyer, Admin or Logistics workspace.</p><form className="form" onSubmit={submit}><label className="field">Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button green" type="submit">Sign in</button></form>{message&&<div className="notice">{message}</div>}<p className="footer">Demo: buyer@farmdirect.demo / FarmDirect2026!</p></section></main>}
