'use client';
import {useState} from 'react';
const steps=['Buyer creates 1,000 kg tomato demand','Prototype demand insight identifies a 0.7 t supply gap','AI-assisted matching ranks eligible farm listings','Three-farm allocation covers the requested quantity','Configured Maharashtra rule assessment completes','Buyer confirms a direct-to-farmer order','Demo 3PL quotes are compared','Prototype route optimization selects pickup sequence','Simulated tracking begins','Delivery completes and feedback updates reliability'];
export default function Demo(){const[step,setStep]=useState(-1);return <main className="app"><nav className="nav"><div className="brand"><i/>FarmDirect</div><span className="pill">SIH JUDGE FLOW</span></nav><section className="hero"><div className="panel"><span className="tag">2-MINUTE GUIDED DEMO</span><h1>See a direct<br/><span>farm-to-buyer</span><br/>transaction.</h1><p>Every simulated value is labeled. The workflow demonstrates matching, allocation, configured compliance and 3PL orchestration without FarmDirect owning produce or vehicles.</p><button className="button green" onClick={()=>setStep(Math.min(step+1,steps.length-1))}>{step<0?'Start demo →':'Next step →'}</button></div><div className="panel flow"><h3>{step<0?'Demo ready':`Step ${step+1} of ${steps.length}`}</h3>{steps.map((x,i)=><div className="node" key={x} style={{opacity:i<=step?1:.35}}><strong>{i<step?'✓ ':i===step?'● ':''}STEP {i+1}</strong>{x}</div>)}</div></section></main>}
