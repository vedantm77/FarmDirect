import type {Demand,Match} from './types';
const base=process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000';
const demo:Match[]=[['Khed Farmer Group (Demo)',420,27,96,42],['Baramati FPO (Demo)',330,26,94,96],['Junnar Growers Collective (Demo)',250,25,92,78]].map((x,i)=>({listing:{id:`l${i+1}`,farmer_name:x[0] as string,crop:'Tomatoes',quantity_kg:x[1] as number,quality_grade:'A',asking_price:x[2] as number,reliability:x[3] as number},distance_km:x[4] as number,score:{overall:96-i*3,distance:94-i*4,price:98,quantity:100,quality:100,readiness:100,reliability:x[3] as number}}));
export async function matchDemand(demand:Demand):Promise<{matches:Match[];fallback:boolean;allocations:{farmer_name:string;quantity_kg:number}[]}> {try{const r=await fetch(`${base}/matching/run`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(demand)});if(!r.ok)throw Error();const data=await r.json();return {...data,fallback:false}}catch{return {matches:demo.filter(x=>x.listing.asking_price<=demand.max_price),allocations:demo.map(x=>({farmer_name:x.listing.farmer_name,quantity_kg:Math.min(300,x.listing.quantity_kg)})),fallback:true}}}
